# tokens.yaml
get:
  description: Return
  operationId: getAdminTokens
  responses:
    '200':
      description: Return masked token with expiration status
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/AdminTokensResponse.yaml'
    default:
      $ref: '../../components/responses/ErrorResponse.yaml'
